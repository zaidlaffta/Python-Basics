#this program to find the min, max, length and sort a list
import random
listx = []
for x in range (34):
    listx.append(x)
print (listx)
print ("The minimum number in the list is: ", min(listx))
print ("The maximum number in the list is: ", max(listx))
print ("the list length is: ", len(listx))
print ("The sorted list is: ", sorted(listx))
